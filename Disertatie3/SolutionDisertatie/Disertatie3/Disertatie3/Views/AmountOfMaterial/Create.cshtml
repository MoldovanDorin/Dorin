@model DisertatieModels.Model.AmountOfMaterial

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AmountOfMaterial</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.RawMaterialsId, "rawMaterials")
        </div>
        <div class="editor-field">
            @Html.DropDownList("RawMaterialsId", String.Empty)
            @Html.ValidationMessageFor(model => model.RawMaterialsId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StockMaterialsId, "stockMaterials")
        </div>
        <div class="editor-field">
            @Html.DropDownList("StockMaterialsId", String.Empty)
            @Html.ValidationMessageFor(model => model.StockMaterialsId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Cantitate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Cantitate)
            @Html.ValidationMessageFor(model => model.Cantitate)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
